# Copy to frontend/.env.local. Next.js reads .env.local from this directory,
# which is why the frontend has its own file rather than sharing backend/.env.

# NEXT_PUBLIC_ is inlined into the browser bundle at build time — it is a public
# value, so nothing secret goes here.
NEXT_PUBLIC_API_URL=http://localhost:8000
